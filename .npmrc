# Handle optional dependencies to prevent platform-specific binary issues
optional=false
omit=optional
# Ensure we don't try to install platform-specific binaries
platform=linux
arch=x64
# Force npm to use specific resolution
legacy-peer-deps=true 